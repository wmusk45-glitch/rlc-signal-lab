# AGENTS.md

## 项目目标

本项目用于制作一个“信号与系统 RLC 电路动态分析”课堂展示网站。

网站定位：

* 用于课堂汇报加分展示
* 优先快速完成、稳定上线
* 同时尽量保证视觉精美和基础交互
* 最终部署到 GitHub Pages

技术路线：

* 只使用 HTML + CSS + JavaScript
* 不使用 React / Vue / Vite
* 不需要后端
* 不需要数据库
* 必须能直接打开 `index.html` 本地预览

## 视觉风格

整体风格：

* 蓝白科研答辩风
* 可加入蓝青绿渐变
* 页面要有轻微高级感
* 使用卡片、渐变、毛玻璃、阴影、按钮上浮、淡入动画
* 不要花哨卡通风
* 不要做成普通资料堆叠页

## 交互要求

网站必须包含一个 RLC 参数交互仿真模块。

用户可以调节：

* 电阻 R
* 电感 L
* 电容 C
* 输入类型：阶跃输入 / 冲激近似输入

网页应实时或点击按钮后更新：

* 输出波形 `v_C(t)`
* 零极点图
* 当前极点数值
* 稳定性说明

默认参数必须为：

* R = 1Ω
* L = 1H
* C = 0.25F

默认结果应对应：

* H(s)=4/(s^2+s+4)
* 极点 s=-0.5±j1.9365

## 计算规则

仿真必须由 JavaScript 真实计算，不允许用图片生成模型伪造。

状态方程：

di_L/dt = (u(t) - R*i_L - v_C) / L

dv_C/dt = i_L / C

极点方程：

s^2 + (R/L)s + 1/(L*C) = 0

如果需要绘制曲线，使用 Canvas 或 SVG 实时绘制。

## 图片生成规则

可以使用 image-2 / Codex 内置图片生成能力制作：

* Hero 背景图
* RLC 电路精美示意图
* 装饰性图标
* 概念链路插图

但不能用 image-2 生成仿真曲线和极点图。

## 文件结构

使用以下结构：

assets/slides/ 存放 12 张 PPT 图片
assets/ 存放 PPT 文件和其他图片资源
index.html 为主页面
style.css 为样式
app.js 为交互逻辑

## 验收标准

完成后必须满足：

1. 双击 `index.html` 可以本地打开。
2. 页面有明显科研风视觉，不是纯文字堆叠。
3. 有淡入、按钮上浮、卡片 hover 等轻交互动画。
4. R、L、C 参数可以调节。
5. 波形图和零极点图会随参数更新。
6. PPT 图片展示区可以查看 12 张幻灯片。
7. 下载按钮可下载 PPT 文件。
8. 所有路径使用相对路径，适合 GitHub Pages。
